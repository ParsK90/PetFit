// ignore: duplicate_ignore
// ignore: file_names
// ignore_for_file: file_names

class Cart {
  // final Pet pet;
  // final int numOfPet;

  // Cart({required this.pet, required this.numOfItem});
}

List<Cart> demoCarts = [
  // Cart(pet: demoPets[0], numOfItem: 2),
  // Cart(pet: demoPets[1], numOfItem: 1),
  // Cart(pet: demoPets[3], numOfItem: 1),
];
